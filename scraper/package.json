{
  "name": "scraper",
  "scripts": {
    "deploy": "firebase deploy",
    "start": "firebase emulators:start",
    "preview": "firebase hosting:channel:deploy beta",
    "build": "node-sass public/scss/main.scss public/css/main.css"
  },
  "engines": {
    "node": "10"
  },
  "private": true,
  "devDependencies": {
    "node-sass": "^5.0.0"
  }
}
